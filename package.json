{
  "name": "tuk-tuk",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:user": "npm run dev -w apps/user",
    "dev:admin": "npm run dev -w apps/admin",
    "convex": "convex dev",
    "convex:deploy": "convex deploy",
    "test": "vitest",
    "test:once": "vitest run",
    "test:coverage": "vitest run --coverage --coverage.reporter=text",
    "seed": "convex run seed:resetAndSeed",
    "clean": "convex run seed:cleanAll",
    "tunnel:user": "npx -y ngrok http 5173",
    "lingui:extract": "lingui extract",
    "lingui:compile": "lingui compile",
    "lingui:extract:watch": "lingui extract --watch"
  },
  "devDependencies": {
    "@edge-runtime/vm": "^4.0.4",
    "@types/node": "^25.2.1",
    "convex": "^1.17.0",
    "convex-test": "^0.0.34",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "dependencies": {
    "convex-helpers": "^0.1.112"
  }
}